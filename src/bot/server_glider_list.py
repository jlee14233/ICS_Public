# 포트폴리오를 위해 실제 IP 주소와 글라이더 리스트는 더미 처리되었습니다.
dockservers = {
    "141": "SERVER_IP_1",
    "142": "SERVER_IP_2",
    "195": "SERVER_IP_3"
}

# 서버별 관제 글라이더 리스트 (더미 데이터)
list141 = ["glider_A", "glider_B"]
list142 = ["glider_C", "glider_D"]
list195 = ["glider_E", "glider_F"]

def get_dockserver_ip(glider_name):
    # 실제로는 글라이더 이름에 따라 dockservers에서 IP를 찾아 반환합니다.
    return ["SERVER_IP_DUMMY"]
